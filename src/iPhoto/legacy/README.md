# Legacy Quarantine

This package contains compatibility-only code that does not conform to the
vNext runtime architecture.

- Production runtime code must not import `iPhoto.legacy`.
- Do not add new functionality here.
- This package is scheduled for deletion in the next major release.
